class Solution:
    def smallerNumbersThanCurrent(self, nums: List[int]) -> List[int]:
        smallernumContainer=[]
        notSortednum=[i for i in nums]
        nums.sort()
        for j in notSortednum:
            n=nums.index(j)
            smallernumContainer.append(n)
        return smallernumContainer
